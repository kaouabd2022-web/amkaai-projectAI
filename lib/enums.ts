import {
  PlanType,
  JobStatus,
  UsageStatus,
  PaymentStatus,
} from "@prisma/client";

export const PLAN = PlanType;
export const JOB_STATUS = JobStatus;
export const USAGE_STATUS = UsageStatus;
export const PAYMENT_STATUS = PaymentStatus;